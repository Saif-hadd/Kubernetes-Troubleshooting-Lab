apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: ktslab
  labels:
    app: webapp
spec:
  type: ClusterIP
  selector:
    app: webapp
  ports:
    - name: http
      port: 8080
      targetPort: 80
      protocol: TCP
