apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: ktslab
  labels:
    app: db
spec:
  type: ClusterIP
  selector:
    app: db
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
      protocol: TCP
